#include <bits/stdc++.h>

int main(){
    int t;
    std::cin >> t;
    while(t--){
        int n;
        std::cin >> n;
        std::vector <int> a(n,0), b(n,0);
        for(int i = 0; i < n; i++){
            std::cin >> a[i];
            b[i] = a[i];
        }
        std::sort(b.begin(), b.end());
        for(int i = 0; i < n; i++){
            
        }
    }
    return 0;
}